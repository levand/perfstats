# perfstats

JMeter plugin for analyzing results using Clojure and Incanter

## Installation

Copy perfstats-1.0.0-SNAPSHOT.jar to JMETER_HOME/lib/ext.

## Compilation

To compile, make sure you have a JMETER_HOME environment variable pointing to your JMeter installation directory - JMeter's jars are required to build